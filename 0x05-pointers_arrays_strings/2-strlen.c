#include "main.h"
#include <string.h>

/**
 * _strlen - function that returns the length of a string
 * @s: char
 * Return: result
 */
int _strlen(char *s)
{
	int len;

	len = strlen(s);
	return (len);
}
